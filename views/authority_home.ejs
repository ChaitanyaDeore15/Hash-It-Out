<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Authority Dashboard</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            padding: 20px;
        }
        h1 {
            color: #333;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 10px;
            text-align: left;
        }
        th {
            background-color: #f4f4f4;
        }
    </style>
</head>
<body>
    <h1>Authority: <%= authority.username %></h1>
    <h2>Email: <%= authority.email %></h2>

    <h2>Complaint List</h2>
    <table>
        <thead>
            <tr>
                <th>Complaint ID</th>
                <th>User ID</th>
                <th>Complaint</th>
                <th>City</th>
                <th>Status</th>
                <th>Update</th>
            </tr>
        </thead>
        <tbody>
            <% complaints.forEach(unique_complaint => { %>
                <tr>
                    <td><%= unique_complaint.complaintid %></td>
                    <td><%= unique_complaint.userid %></td>
                    <td><%= unique_complaint.complaint %></td>
                    <td><%= unique_complaint.city %></td>
                    <td><%= unique_complaint.status %>
                        <form method="post" action="/<%= authority.authorityid %>/<%= unique_complaint.complaintid %>/upated">
                            <button>Completed</button>
                        </form>
                    </td>
                    <td>
                        
                    </td>
                </tr>
            <% }); %>
        </tbody>
    </table>
</body>
</html>
